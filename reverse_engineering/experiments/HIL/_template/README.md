# [reverse_engineering 단위 HIL 템플릿] `<topic>`

> `_template/` → `YYYY-MM-DD_<topic>/` 복사 후 작성. HIL = 실제 Claude Code 런타임("하드웨어" = 하네스). 완료 후 상위 `../../../../experiments/INDEX.md` §2 갱신.

## 목적

실 설치 후 활성화 게이트(`principle.md`)로 룰이 활성화되고 RE/구조/검증 트리거 시 세션에 주입되는가. §6 라벨(`[존재]`/`[동작]`) 적용·동작 주장 배포자산 대조 게이트 준수.

## 환경

- 타깃 프로젝트 / commit:
- 설치: `./reverse_engineering/install.sh <target>`

## 실행 절차

1. 타깃에 `install.sh` 설치 → 세션 시작.
2. 룰 트리거(RE·재구현·구조 분석·검증) → 규칙 활성·§6 라벨 분리·동작 주장 대조 확인.

## 결과

| 항목 | 관찰 | 기대 | 판정 |
|------|------|------|------|
| 게이트 활성화 | — | `principle.md` 존재 시 룰 활성 | ⏳ |
| 규칙 주입 | — | 세션 컨텍스트 반영 | ⏳ |
| 존재 vs 동작 분리 | — | 동작 주장에 배포자산/도달성 인용 동반 | ⏳ |

## 정리

(원본 100% 동일 + 존재/동작 분리 준수)
