<!-- kuks_agent_setup:computer_use -->
- PC 화면 읽기·분석·조작(read→analyze→act→re-read): `computer-use` 스킬(쓰기) + `capture-test` 스킬(읽기). 실행기 `~/.claude/computer_action.py`(입력)·`~/.claude/capture_screen.py`(캡처). 사용자가 부를 때만 동작(on-demand), 입력 후 피드백으로 안전 확보.
