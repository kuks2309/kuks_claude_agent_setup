<!-- kuks_agent_setup:hwpx -->
- 한글(HWP/HWPX) 문서 생성·편집 + PDF 렌더 검증: `hwpx` 스킬(`~/.claude/skills/hwpx`). 양식 치환으로 문서를 만들고, 완료 선언 전 반드시 `render_verify.py` 로 PDF→PNG 렌더를 눈으로 검토(서식·글자 깨짐·디자인)하는 루프를 통과시킨다.
